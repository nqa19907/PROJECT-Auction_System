package auction_system.client.patterns.singleton;

/**
 * Lớp quản lý kết nối tới máy chủ áp dụng mẫu thiết kế Singleton.
 */
public class ServerConnection {
    private ServerConnection() {
        // Ngăn chặn khởi tạo lớp tiện ích từ bên ngoài
    }
}
